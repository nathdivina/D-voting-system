# Voting System (PyQt6 + SQLite) - FOP structured example

Run with Python 3.10+. Install PyQt6 via `pip install PyQt6`.

Run:

```
python main.py
```

Admin login username: `admin` password: `Admin@123456`

This is a demo scaffold. For production, secure admin credentials and password storage appropriately.
